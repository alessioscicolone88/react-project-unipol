import BootstrapServer from "./bootstrapServer";

describe("tpd tpd schema org bootstrapServer Export", () => {
  it("Testing export of method", () => {
    expect(BootstrapServer).toBeDefined();
  });
});
