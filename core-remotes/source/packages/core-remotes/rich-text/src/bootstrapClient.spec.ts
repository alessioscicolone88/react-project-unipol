import * as BootstrapClient from "./bootstrapClient";

describe("Remotes rich-text bootstrapClient Export", () => {
  it("Testing export of method", () => {
    expect(BootstrapClient).toBeDefined();
  });
});
